# NL2SQL Phase 1 — Python dependencies
# Generated from conda env: NL2SQL (Python 3.11, Apple Silicon / M-series)
# Install: pip install -r requirements.txt
# Then install spaCy model: python -m spacy download en_core_web_sm

# ── Core pipeline ──────────────────────────────────────────────────────────────
anthropic>=0.84.0
sentence-transformers>=5.0.0
faiss-cpu>=1.8.0
datasketch>=1.6.0
spacy>=3.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
pandas>=2.0.0
tqdm>=4.0.0
tenacity>=8.0.0
tiktoken>=0.6.0
numpy>=2.0.0
httpx>=0.27.0
diskcache>=5.0.0

# ── Gemini provider (optional) ─────────────────────────────────────────────────
google-genai>=1.0.0
google-auth>=2.0.0
protobuf>=4.0.0

# ── MLX local provider (Apple Silicon only, optional) ─────────────────────────
openai>=2.0.0
instructor>=1.3.0
mlx>=0.20.0
mlx-lm>=0.20.0
mlx-metal>=0.20.0

# ── sentence-transformers / HuggingFace deps ───────────────────────────────────
transformers>=4.40.0
tokenizers>=0.19.0
huggingface_hub>=0.20.0
safetensors>=0.4.0
sentencepiece>=0.1.99
torch>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# ── Dev / testing ──────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
